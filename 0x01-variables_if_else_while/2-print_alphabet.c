#include <stdio.h>

/**
 * main - main function
 * Description:Printing alphabets in lowercase
 * Return: Return
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
		putchar('\n');
	return (0);
}
